import React from "react";
import { StreamManagerView } from "@/features/stream-manager/components/StreamManagerView";

interface StreamManagerPageProps {}

const StreamManagerPage = ({}: StreamManagerPageProps) => {
  return <StreamManagerView />;
};

export default StreamManagerPage;
